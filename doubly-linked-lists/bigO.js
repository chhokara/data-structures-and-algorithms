// Big O of Doubly Linked Lists
// Insertion - O(1)
// Removal - O(1)
// Searching - O(N)
// Access - O(N)

// Technically searching is O(N/2) but that is still O(N)

// Recap
// Doubly linked lists are similar to singly linked lists except they have an 
// additional pointer to previous nodes