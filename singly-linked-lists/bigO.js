// Insertion - O(1)

// Removal depends on where we are removing from
// if removal at very front - O(1)
// if removal at very end - O(N)

// Searching - O(N)

// Access - O(N)

// Recap
// Singly Linked lists are an excellent alternative to arrays when insertion and deletion
// at the beginning are frequently 

// Arrays contain a built in index whereas linked lists do not

// The idea of a list data structure that consists of nodes is the foundation for 
// other data structures like stacks and queues